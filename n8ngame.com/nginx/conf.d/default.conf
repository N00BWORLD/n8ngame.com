server {
    listen 80;
    server_name example.com; # CHANGE THIS TO YOUR DOMAIN

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        # Uncomment the following line to redirect to HTTPS after setting up certificates
        # return 301 https://$host$request_uri;
        
        # For initial setup (HTTP only), proxy to app:
        proxy_pass http://app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # location /api/ {
    #     proxy_pass http://backend:3000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
}

# Uncomment this block AFTER getting certificates
# server {
#     listen 443 ssl;
#     server_name example.com; # CHANGE THIS TO YOUR DOMAIN
# 
#     ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem; # CHANGE example.com
#     ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem; # CHANGE example.com
# 
#     location / {
#         proxy_pass http://app;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # location /api/ {
#     #     proxy_pass http://backend:3000/;
#     #     proxy_set_header Host $host;
#     #     proxy_set_header X-Real-IP $remote_addr;
#     # }
# }
